
void mayorCantInvolucrados(int numConsejos, struct consejo Consejos[3],struct NombresC consejos[]);
void satisfacerDemanda(int numConsejos, struct consejo Consejos[3],struct camion camiones[]);
int satisfacerSolicitudes(int numConsejos, struct consejo Consejos[3],struct camion camiones[]);
int mayorCapacidad(struct camion camiones[]);
